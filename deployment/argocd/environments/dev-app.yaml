apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zero-to-kanban-test-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/julian52575/Zero-To-Kanban-Test.git
    targetRevision: main
    path: deployment/helm/zero-to-kanban
    helm:
      valueFiles:
        - values.yaml
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: zero-to-kanban-test-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
